// Compact step header component for wizard steps (no decorative elements)
export const WizardStepArt = () => {
  // Component removed - no longer displays anything
  // Kept for backward compatibility with existing imports
  return null;
};
